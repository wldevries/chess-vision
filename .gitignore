# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv / virtualenv
.venv/

# Trained model weights (track via DVC/releases, not git)
models/*.pt
models/*.pth
models/*.onnx

# Self-check / experiment outputs
runs/

# Ultralytics auto-downloads bare COCO-pretrained weights into CWD; re-fetched on
# demand, so don't commit them (our trained checkpoints live under runs/, ignored above).
yolo*.pt

# Local environment config
.env

# OS / editor
.DS_Store
*.swp

# Datasets and captured photos (top-level only; keep packaged sample PGNs)
/data/
/games/
# geometry validation reference (regenerated by scripts/validate_geometry_js.py)
web/_geom_ref.json
